{
  "name": "mcpwall",
  "version": "0.2.0",
  "description": "Deterministic security proxy for MCP tool calls. Blocks dangerous requests, redacts secrets from responses, catches prompt injection. No AI, no cloud, pure rules.",
  "type": "module",
  "bin": {
    "mcpwall": "dist/index.js"
  },
  "files": [
    "dist",
    "rules"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "prepublishOnly": "npm run build && chmod +x dist/index.js"
  },
  "keywords": [
    "mcp",
    "firewall",
    "security",
    "proxy",
    "model-context-protocol",
    "claude",
    "ai-safety",
    "tool-call",
    "json-rpc",
    "audit"
  ],
  "author": "Dom Behrens",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/behrensd/mcpwall.git"
  },
  "mcpName": "io.github.behrensd/mcpwall",
  "homepage": "https://mcpwall.dev",
  "bugs": {
    "url": "https://github.com/behrensd/mcpwall/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "minimatch": "^10.2.0",
    "yaml": "^2.8.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/minimatch": "^5.1.2",
    "@types/node": "^25.2.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
